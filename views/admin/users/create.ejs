<%- include("../../partials/header.ejs") %>
<body id="login">
<%- include("../../partials/navbar.ejs") %>
    <div class="container">
        
        <div class="card mt-5 card_login">
            <div class="card-header">
                <h3>Criação de usuario</h3>
            </div>
            <div class="card-body">
                <form class="form_login" method="POST" action="/user/create">
                    <% if (typeof erro != 'undefined') { %>
                        <div class="alert alert-danger" role="alert">
                            <%= email + erro %>
                        </div>
                    <% } %>
                    <p>
                        <input type="email" class="form-control" name="email" placeholder="email@provedor.com.br">
                    </p>
                    <p>
                        <input type="password" class="form-control" name="password" placeholder="************">
                    </p>
                    <button type="submit" class="btn btn-success btn-block">Criar</button>
                </form>
            </div>
        </div>
    </div>
</body>
<%- include("../../partials/footer.ejs") %>